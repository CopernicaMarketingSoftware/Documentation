config options to add to documentation:
    "server-id",
    "max-threads",
    "max-connections",
    "max-messages",
    "max-queues",
    "max-memory",
    "www-host",
    "smtp-privatekey", < is this the privatekey from mgmt-dkim?
    "dns-threads",
    "config-file",
    "envelope"


how many attempts?  
unlimited in json-messages   maxattempts
6         in mime-headers    x-mq-maxattempts


possibly missing from config file: 
a default value for x-mq-delayed
because how formulated in text, but does that even make sense contextwise

nieuwe opdracht
